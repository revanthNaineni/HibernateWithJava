<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database Connection Settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/hibernate_prep</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <!-- Specify the SQL dialect for your database -->
        <!-- <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property> -->

        <!-- Enable Hibernate's automatic schema generation (for testing purposes) -->
        <!-- <property name="hibernate.hbm2ddl.auto">update</property> -->

        <!-- Echo all executed SQL to the console for debugging -->
        <property name="hibernate.show_sql">true</property>
        
        <!-- Use the second-level cache for entity caching (optional) -->
        <!-- <property name="hibernate.cache.use_second_level_cache">true</property> -->

        <!-- Package containing your annotated entity classes -->
        <!-- <mapping class="org.hibernatePrep" /> -->
        <!-- Add more <mapping> elements for additional entity classes -->
    </session-factory>
</hibernate-configuration>
